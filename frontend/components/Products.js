import React, { Component } from 'react';

class Products extends Component {
    render() {
        return (
            <div>
                this is where all the products are
            </div>
        );
    }
}

export default Products;