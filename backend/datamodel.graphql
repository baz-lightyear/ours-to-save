type User {
  id: ID! @id
  name: String!
  email: String! @unique
  password: String!
  resetToken: String
  resetTokenExpiry: Float
}

type Story {
    id: ID! @id
    title: String!
    content: String
}

type Initiative {
    id: ID! @id
    title: String!
    description: String
}