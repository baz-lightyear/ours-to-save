type Mutation {
    createStory(title: String!, content: String!, address: String!, good: String): Story
}

type Query {
    stories: [Story]!
}

type Story {
    id: ID
    title: String
    content: String
    good: String
    longitude: Float
    latitude: Float
}
